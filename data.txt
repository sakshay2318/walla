The script scans JavaScript and JSX files to identify instances of missing or improper role-based access control (RBAC) validations using predefined patterns. Matches are logged with corresponding file paths and line numbers.
